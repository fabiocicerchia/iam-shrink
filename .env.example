# Copy to .env and fill in. Never commit the real .env.
# iam-shrink reads AWS credentials from the standard boto3 chain
# (env vars, shared config, IAM roles). Set these only for local runs.
# AWS_PROFILE=
# AWS_REGION=
